# Extra dependencies for the esg_regime package (on top of the repo root
# requirements.txt, which already pins numpy / pandas / yfinance).
hmmlearn>=0.3
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
